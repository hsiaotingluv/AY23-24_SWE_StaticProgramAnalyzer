#include "PKB.h"

#include <cstdio>

#include <iostream>
#include <string>
#include <vector>

#include "TNode.h"

auto PKB::setProcToAST(PROC p, TNode *r) -> int {
    return 0;
}

auto PKB::getRootAST(PROC p) -> TNode * {
    return nullptr;
}
